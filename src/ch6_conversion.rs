fn main() {
    // primitive types can be converted through casting
    // conversion between custom types is done using Traits
    // the generic conversions use the From and Into traits
    // but there are more specific ones for the more commons cases,
    // especially when converting between Strings.
}
